# Example environment (no secrets)
TZ=Asia/Seoul
MSSQL_SA_PASSWORD=CHANGE_ME

# Optional runtime docs location used by clients for /mcp/standardize/spec-with-evidence payloads
# docs_dir is a request option, not a required server env var.
